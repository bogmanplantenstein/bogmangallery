# Credentials — NEVER commit these
credentials/
.env

# Node
node_modules/
npm-debug.log*

# Local backups (snapshots kept only on disk, not in repo)
data/*.bak
data/*.backup.json
data/backups/*.json

# Map embed PNGs (generated externally by climate map snapshot script)
mapEmbed/*.png

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp
